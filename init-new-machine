brew update
sudo brew install git neovim chromium curl tmux ripgrep fzf lynx

sudo apt update
sudo apt install git neovim ubuntu-tweaks curl tmux ripgrep fzf lynx 

git clone https://github.com/magicmonty/bash-git-prompt.git ~/.bash-git-prompt --depth=1

ssh-keygen -t rsa -b 4096 -C "marinovboris@protonmail.com"
mkdir ~/pr

npm install -g spellchecker-cli

cd ~/.config
mkdir nvim
cd nvim/
ln -s ~/bin/.vimrc init.vim
ln -s ~/bin/.tmux.conf ~
ln -s ~/bin/.tmux.conf ~

